# CC64サスティンペダルがDAWで再生されない問題の調査とトラブルシューティング

## 問題状況
- `insert_sustain`機能でCC64（サスティンペダル）イベントが正しくMIDIファイルに挿入される
- MIDIファイルのバイナリレベルでもCC64イベント（B0 40 7F, B0 40 00）が確認できる
- しかし、DAWでの再生時にサスティンペダル効果が適用されない

## 考えられる原因と対策

### 1. DAW側のMIDI CC設定
**問題：** DAWがCC64を認識しているが、実際の音響処理に反映されていない
**対策：**
- DAWのMIDI CC学習/マッピング機能でCC64を手動で割り当て
- Logic Pro: エンバイロメント→MIDIインストゥルメント→CC64を確認
- Pro Tools: MIDIエディター→CC64レーンを表示して確認
- Cubase: キーエディター→コントローラーレーン→CC64を追加

### 2. 音源/プラグインのCC64対応
**問題：** 使用している音源がCC64（サスティンペダル）に対応していない
**対策：**
- 異なる音源/プラグインでテスト（EXS24、Kontakt、Pianoteq等）
- ソフトウェアピアノ音源を使用してテスト
- CC64対応を明記している音源を使用

### 3. MIDIチャンネル不整合
**問題：** CC64が送信されるチャンネルと音源が受信するチャンネルが一致しない
**対策：**
- 音源の受信チャンネルとCC64のチャンネルを明示的に合わせる
- 全チャンネル（0-15）にCC64を送信するテスト版を作成

### 4. タイミング問題
**問題：** CC64のON/OFFタイミングが音符のタイミングと適切に同期していない
**対策：**
- CC64 ONをノートオン直前（例：-1tick）に配置
- CC64 OFFをノートオフ直後（例：+1tick）に配置
- より細かいタイミング調整

### 5. CC64値の問題
**問題：** 一部のDAW/音源でCC64の127/0以外の値が必要
**対策：**
- 64以上の値をONとして試行（例：64, 100, 127）
- ハーフペダル値のテスト（例：64でON、0でOFF）

## テストファイル生成
以下のコマンドで複数パターンのテストファイルを生成して確認してください：

```bash
# 全チャンネルCC64テスト用
node create_cc64_multichannel_test.js

# タイミング調整テスト用  
node create_cc64_timing_test.js

# 異なるCC64値テスト用
node create_cc64_value_test.js
```

## 推奨検証手順
1. 作成したcc64_daw_test.midをDAWにインポート
2. MIDIエディターでCC64イベントが表示されることを確認
3. 複数の音源プラグインでテスト
4. CC64レーンを表示して手動でCC64イベントを追加・比較
5. リアルタイムペダルコントローラーとの動作比較

## 既知の制限事項
- 一部のDAWはCC64をリアルタイム入力のみ対応
- 音源によってはCC64実装が不完全
- MIDI規格準拠でもDAW固有の解釈が存在

このファイルを参考に、段階的に問題を特定してください。